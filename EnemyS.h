#include "Entity.h"

#include <vector>

class EnemyS : public Entity
{
public:
	EnemyS();
	~EnemyS();

	//member methods:
	bool Initialise(Renderer& renderer);

protected:

private:


	//member data:
public:

protected:

private:

};